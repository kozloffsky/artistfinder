Acted\LegalDocsBundle\Entity\Event:
    exclusion_policy: NONE
    properties:
        id:
            groups: [chat_list, getEvent]
        country:
            groups: [Default, getEvent]
        city:
            groups: [Default, getEvent, chat_list]
        eventRef:
            groups: [Default, getEvent]
        eventTypeId:
            groups: [Default, getEvent, chat_list]
        venueTypeId:
            groups: [Default, getEvent]
        startingDate:
            groups: [Default, getEvent, chat_list]
            type: DateTime<"m/d/Y">
        endingDate:
            groups: [Default, getEvent]
        timing:
            groups: [Default, getEvent, chat_list]
        comments:
            groups: [Default, getEvent, chat_list]
        numberOfGuests:
            groups: [getEvent]
        venueType:
            groups: [getEvent]
        eventType:
            groups: [getEvent]
        title:
            groups: [Default, getEvent, chat_list]
        address:
            groups: [Default, getEvent, chat_list]
            serialized_name: location
        user:
            groups: [chat_list, chat_room]
    virtual_properties:
        getCountryId:
            serialized_name: countryId
            groups: [getEvent]
