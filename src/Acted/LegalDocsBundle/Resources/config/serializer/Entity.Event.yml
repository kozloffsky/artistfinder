Acted\LegalDocsBundle\Entity\Event:
    exclusion_policy: NONE
    properties:
        id:
            groups: [chat_list, getEvent, enquiries, messages_feedbacks, all_messages]
        city:
            groups: [Default, getEvent, chat_list, chat_room, all_messages]
        eventRef:
            groups: [Default, getEvent]
        eventTypeId:
            groups: [Default, getEvent, chat_list, chat_room]
        venueTypeId:
            groups: [Default, getEvent]
        startingDate:
            groups: [Default, getEvent, chat_list, chat_room, all_messages]
        endingDate:
            groups: [Default, getEvent]
        timing:
            groups: [Default, getEvent, chat_list, chat_room]
        comments:
            groups: [Default, getEvent, chat_list, chat_room]
        numberOfGuests:
            groups: [getEvent, chat_room]
        venueType:
            groups: [getEvent, chat_room]
        eventType:
            groups: [getEvent, chat_room]
        title:
            groups: [Default, getEvent, chat_list, chat_room, all_messages]
        address:
            groups: [Default, getEvent, chat_list, chat_room]
            serialized_name: location
        user:
            groups: [chat_list, getEvent, messages_feedbacks]
        requestQuotations:
            groups: [chat_room]
        eventOffer:
          groups: [getEvent]
        countDays:
          groups: [getEvent]
    virtual_properties:
        getCountryId:
            serialized_name: countryId
            groups: [getEvent]
        getCountryName:
            serialized_name: countryName
            groups: [chat_room]
